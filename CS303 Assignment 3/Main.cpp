// Name: Alyssa Barbee
// Date: 12/6/24
// CS303 Assignemt 3
// Credit: Chatgpt and peer for helping me fix up some of my code

//#include "Q1Function.h"
//#include "Q2Function.h"
//#include "Q3Function.h" 

int main()
{
  
    /*
    
    //Create queue
    queue<int> my_queue;

    //Initialize queue with integers
    for (int i = 0; i < 11; i++) {
        my_queue.push(i);
    }

    
    cout << "Originial List:" << endl;
    my_queue.print_list(); //print original list 
    cout << endl << endl;
    my_queue.move_to_rear(); //move the first value of the queue to the end of the queue
    cout << "Modified List: " << endl;
    my_queue.print_list(); //print modified list 
    */

    /*
    //create vector
    vector<int> my_vector;

    //initialze vector with integers
    my_vector = { 0,10,75,23,75 };

    //Find the last occurrence of the target integer
    int target = 75;
    int position = linear_search(my_vector, target, my_vector.size() - 1);
    if (position != -1)
        cout << "Last occurrence of " << target << " is at position " << position << endl;
    else
        cout << "Target not found in the vector." << endl;
    */
    
    /*
    //Create list
    list<int> my_list;

    //Initialze list with values
    my_list.push_back(10);
    my_list.push_back(75);
    my_list.push_back(33);
    my_list.push_back(1);
    my_list.push_back(45);

    //Display original list
    cout << "Original Linked List/Queue: " << endl;
    for (int val : my_list)
        cout << val << " ";
    cout << endl;

    //Sort list using insertion sort
    insertion_sort(my_list); 

    return 0;
    */
}